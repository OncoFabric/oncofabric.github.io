---
interface Props {
  title: string;
  authors: string[];
  journal: string;
  year: number;
  category: string;
  doi?: string;
  url?: string;
  abstract?: string;
  preprint?: boolean;
}

const { title, authors, journal, year, category, doi, url, abstract, preprint } = Astro.props;
const linkHref = url || (doi ? `https://doi.org/${doi}` : '');
---

<div class="publication-card" data-year={year} data-category={category} data-preprint={preprint ? "true" : "false"}>
  <div class="publication-header">
    {linkHref ? (
      <a href={linkHref} target="_blank" rel="noopener noreferrer" class="publication-title">{title}</a>
    ) : (
      <span class="publication-title no-link">{title}</span>
    )}
    <div class="publication-badges">
      {preprint && <span class="publication-preprint">Preprint</span>}
      <span class="publication-category">{category}</span>
    </div>
  </div>
  <p class="publication-authors">{authors.join(', ')}</p>
  <p class="publication-journal">
    <span class="journal-name">{journal}</span> &middot; {year}
    {doi && <> &middot; <a href={`https://doi.org/${doi}`} target="_blank" rel="noopener noreferrer" class="doi-link">DOI</a></>}
  </p>
  {abstract && <p class="publication-abstract">{abstract}</p>}
</div>

<style>
  .publication-card {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 20px 22px;
    transition: transform 0.15s ease, border-color 0.2s;
  }
  .publication-card:hover {
    transform: translateY(-2px);
    border-color: var(--border-strong);
  }
  .publication-card[data-hidden="true"] {
    display: none;
  }
  .publication-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 6px;
  }
  .publication-title {
    font-size: 16px;
    font-weight: 700;
    letter-spacing: -0.01em;
    color: var(--teal);
    text-decoration: none;
    line-height: 1.3;
  }
  .publication-title:hover { text-decoration: underline; }
  .publication-title.no-link { color: var(--text-primary); }
  .publication-badges {
    display: flex;
    gap: 6px;
    flex-shrink: 0;
    align-items: flex-start;
  }
  .publication-category {
    font-size: 11px;
    font-family: var(--mono);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--teal);
    background: var(--teal-glow);
    padding: 3px 8px;
    border-radius: 4px;
    white-space: nowrap;
  }
  .publication-preprint {
    font-size: 11px;
    font-family: var(--mono);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--coral);
    background: color-mix(in srgb, var(--coral) 12%, transparent);
    padding: 3px 8px;
    border-radius: 4px;
    white-space: nowrap;
  }
  .publication-authors {
    font-size: 13px;
    color: var(--text-secondary);
    margin-bottom: 4px;
  }
  .publication-journal {
    font-size: 12px;
    font-family: var(--mono);
    color: var(--text-tertiary);
  }
  .journal-name { font-style: italic; font-family: var(--sans); }
  .doi-link {
    color: var(--teal);
    text-decoration: none;
    font-weight: 600;
  }
  .doi-link:hover { text-decoration: underline; }
  .publication-abstract {
    font-size: 13px;
    font-weight: 300;
    line-height: 1.6;
    color: var(--text-secondary);
    margin-top: 10px;
    padding-top: 10px;
    border-top: 1px solid var(--border);
  }
</style>
